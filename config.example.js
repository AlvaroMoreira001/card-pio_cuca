// Modelo de config.js (que é gerado automaticamente por build.js e não vai pro git).
// Para rodar localmente sem Node: copie este arquivo para config.js.
// Para publicar com o número real, defina WHATSAPP_NUMBER no Vercel/​.env — nunca
// edite o número real diretamente aqui.
window.APP_CONFIG = {
  whatsappNumber: "5511999999999"
};
